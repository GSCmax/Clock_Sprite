<Window x:Class="Clock_Sprite.View.MainSprite"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Clock_Sprite.View"
        mc:Ignorable="d"
        Title="Clock Sprite"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Topmost="True"
        local:WindowAttach.ShowInTaskManager="False"
        local:WindowAttach.IgnoreAltF4="True"
        local:WindowAttach.IsDragElement="True">
    <Border Background="{StaticResource BackgroundBrush}" Effect="{StaticResource EffectShadow}" CornerRadius="0,11,11,11" Margin="25">
        <local:SimplePanel>
            <TextBlock Name="clockTB" FontSize="28" Text="00:00:00" Foreground="White" Padding="8,4" MouseWheel="clock_MouseWheel"
                       ToolTipService.ShowDuration="60000" ToolTipService.HorizontalOffset="-25" ToolTipService.VerticalOffset="-25">
                <TextBlock.ToolTip>
                    <Border Background="{StaticResource BackgroundBrush}" Effect="{StaticResource EffectShadow}" CornerRadius="11" Margin="25">
                        <TextBlock FontSize="14" Foreground="White" Padding="8,4,0,4">
                            <Bold>Clock Sprite 使用技巧：</Bold><LineBreak/>
                            1.软件启动时位于每个显示器右上角且<Run FontWeight="ExtraBold">窗口置顶</Run>；<LineBreak/>
                            2.鼠标左键按住窗口任意位置拖动窗口；<LineBreak/>
                            3.鼠标左键点击<Run Foreground="{StaticResource HighlightBrush}" FontWeight="ExtraBold">"i"</Run>按钮切换窗口是否置顶；<LineBreak/>
                            4.鼠标左键双击<Run Foreground="{StaticResource HighlightBrush}" FontWeight="ExtraBold">"i"</Run>按钮显示<Run Foreground="{StaticResource DangerBrush}" FontWeight="ExtraBold">"关闭"</Run>按钮；<LineBreak/>
                            5.在<Run Foreground="{StaticResource HighlightBrush}" FontWeight="ExtraBold">"i"</Run>按钮区域滚动鼠标滚轮调整窗口透明度；<LineBreak/>
                            6.在<Run FontWeight="ExtraBold">时间显示</Run>区域滚动鼠标滚轮调整时间显示大小。
                        </TextBlock>
                    </Border>
                </TextBlock.ToolTip>
            </TextBlock>
            <Button Name="exitBTN" Visibility="Collapsed"
                    Width="22" Height="22" Style="{StaticResource ButtonIcon}"
                    Foreground="{StaticResource DangerBrush}" Background="White" Click="exit_Click" MouseLeave="exit_MouseLeave"
                    local:BorderElement.CornerRadius="11"
                    local:IconElement.Geometry="{StaticResource ErrorGeometry}" Padding="-1"
                    HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-9,-9,0,0"/>
            <Button Name="infoBTN" Visibility="Visible"
                    Width="22" Height="22" Style="{StaticResource ButtonIcon}"
                    Foreground="{StaticResource HighlightBrush}" Background="White" Click="info_Click" MouseDoubleClick="info_MouseDoubleClick" MouseWheel="info_MouseWheel"
                    local:BorderElement.CornerRadius="11"
                    local:IconElement.Geometry="{StaticResource InfoGeometry}" Padding="-1"
                    HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-9,-9,0,0">
            </Button>
        </local:SimplePanel>
    </Border>
</Window>
